git checkout -b nowy-branch
echo "nowy branch" >> test.txt
git add test.txt
git cinit -m "Dodanie linijki na nowym branchu"
git push origin nowy-branch
git checkout main
echo "główny branch" >> test.txt
git add test.txt
got commit -m "Dodanie linijki na głównym branchu"
git merge nowy-branch
git add test.txt
git commit -m "Merge brancha - zachowanie"
